# TODO

All phases are complete. Remaining items are stretch goals / hardening.

## Edge Cases (nice to have)
- [ ] Handle remote no longer exists on `catalog add` (warn, register local)
- [ ] Offer `mw ls --prune` to clean up orphaned worktrees
